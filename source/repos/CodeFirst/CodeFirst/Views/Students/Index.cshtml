@{
    var students = (List<StudentModel>)ViewBag.Students;
    string mail = (string)ViewBag.mail;
    string firstname = (string)ViewBag.firstname;
    var ides = (List<StudentModel>)ViewBag.ids;
    var dt = (StudentModel)ViewData["detail"];
}

<h1>@mail</h1>
<h1>@firstname</h1>
<h3>Details of student:</h3>
<p>Hi my name is @dt.fn, and my last name is @dt.ln .My email is @dt.email </p>

@*<table class="table">
        <tr>

                <th>Id</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Email</th>

        </tr>
        <tbody>
            @foreach (var a in students)
            {
                <tr>
                    <td>
                        @a.id
                    </td>
                    <td>
                        @a.fn
                    </td>
                    <td>
                        @a.ln
                    </td>
                    <td>
                        @a.email
                    </td>
                </tr>
            }
        </tbody>
    </table>*@

